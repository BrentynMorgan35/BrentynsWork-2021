using System;

namespace CS_ASP_30Ticket
{
    class Program
    {
        static void Main(string[] args)
        {
            int studentNum;
            int speedLimit;
            int ticketSpeed;
            double price;


        Console.Write("Enter Student Number: ");
            studentNum = int.Parse(Console.ReadLine());
        Console.Write("Eneter speed limit");
            speedLimit = int.Parse(Console.ReadLine());
        Console.Write("Enter speed of student");
            ticketSpeed = int.Parse(Console.ReadLine());
        Console.Write("Enter class of student by number shown- 0 Freshman, 1 Sophmore, 2 Jumior, 3 Senior");
            switch (int.Parse(Console.ReadLine());
            {
                case 0:


                default:
                    break;
            }




        }
    }
}
